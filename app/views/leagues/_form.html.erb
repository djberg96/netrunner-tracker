
  <div class="group">
    <%= f.label :name, t("activerecord.attributes.league.name", :default => "Name"), :class => :label %>
    <%= f.text_field :name, :class => 'text_field' %>
    <span class="description">Max. 32 characters.</span>
  </div>

  <div class="group">
    <%= f.label :comments, t("activerecord.attributes.league.comments", :default => "Comments"), :class => :label %>
    <%= f.text_area :comments, :class => 'text_area', :rows => 5 %>
    <span class="description">Location, typical meetup day, etc.</span>
  </div>
        
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  </button>
  <span class="text_button_padding"><%= t("web-app-theme.or", :default => "or") %></span>
	<%= link_to t("web-app-theme.cancel", :default => "Cancel"), leagues_path, :class => "text_button_padding link_button" %>
</div>
