<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.collection_select :kind, Card::TYPES, :to_s, :to_s %>
  </div>
  <div class="field">
    <%= f.label :faction %><br />
    <%= f.collection_select :faction, Card::FACTIONS, :to_s, :to_s %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
