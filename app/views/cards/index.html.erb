<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", cards_path  %></li>
      <% if current_user.admin? %>
        <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_card_path %></li>
      <% end %>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Cards</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th><%= t("activerecord.attributes.card.title", :default => t("activerecord.labels.title", :default => "Title")) %></th>
          <th><%= t("activerecord.attributes.card.faction", :default => t("activerecord.labels.faction", :default => "Faction")) %></th>
          <th><%= t("activerecord.attributes.card.kind", :default => t("activerecord.labels.kind", :default => "Type")) %></th>
        </tr>
        <% @cards.each do |card| -%>
        <tr class="<%= cycle("odd", "even") %>" onclick="window.location.href = '<%= card_path(card) %>'">

          <td><%= card.title %></td>
          <td><%= card.faction %></td>
          <td><%= card.kind %></td>
        </tr>          
        <% end -%>
      </table>
      <p>
        <%= link_to_previous_page @cards, 'Previous' %>
        <%= link_to_next_page @cards, 'Next' %>
      </p>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
